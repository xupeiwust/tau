/**
 * Geometry analysis, grading, and test utilities for `@taucad` packages.
 *
 * - Test schemas and types (requirements, results, test files)
 * - Geometry analysis functions (`analyzeGlb`, `evaluateRequirement`, etc.)
 *
 * @module
 */
export * from '#schemas.js';
export * from '#geometry/index.js';

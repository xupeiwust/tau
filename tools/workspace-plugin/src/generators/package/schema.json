{
  "$schema": "https://json-schema.org/schema",
  "$id": "TauPackage",
  "title": "Create a @taucad/* package",
  "description": "Scaffolds a publishable @taucad/* package with dual ESM/CJS build, vitest, and all Tau conventions.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Package name without the @taucad/ prefix (e.g. 'react' → @taucad/react)",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the package name (without @taucad/ prefix)?",
      "x-priority": "important"
    },
    "description": {
      "type": "string",
      "description": "Package description for package.json and README",
      "default": ""
    },
    "scope": {
      "type": "string",
      "enum": ["packages", "libs"],
      "default": "packages",
      "description": "Directory scope (packages for publishable, libs for internal)"
    }
  },
  "required": ["name"]
}
